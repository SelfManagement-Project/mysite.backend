<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mysite.web.ai.mapper.ChatHistoryMapper">

    <select id="getChatHistoriesByUserId" resultType="com.mysite.web.ai.dto.ChatHistoryResponse">
        SELECT history_id, chat_id, session_id, message_type, content, created_at
        FROM chat_history
        WHERE chat_id IN (SELECT chat_id FROM chat WHERE user_id = #{userId})
        <if test="search != null and search != ''">
            AND content ILIKE CONCAT('%', #{search}, '%')
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
